apply plugin: 'com.android.library'
apply plugin: 'maven'
group = 'org.jraf'
version = '1.0.0'

android {
    compileSdkVersion 26
    buildToolsVersion '26.0.1'

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 26
    }

    buildTypes {
        release {
            // Do not run proguard for this library: it would strip all code since it is not called anywhere
            minifyEnabled false
        }
    }

    dataBinding {
        enabled = true
    }
}

dependencies {
    implementation 'com.android.support:appcompat-v7:26.0.2'
    implementation 'com.jakewharton.timber:timber:4.5.1'
    implementation files('libs/http-request-5.3-SNAPSHOT.jar')

    // Wear
//    compileOnly 'com.android.support:wear:26.0.2'
    compileOnly 'com.google.android.support:wearable:2.0.5'
    compileOnly 'com.google.android.wearable:wearable:2.0.5'
}

uploadArchives {
    repositories {
        mavenDeployer {
            repository url: mavenLocal().getUrl()
            pom.artifactId = 'jraf-android-util'
        }
    }
}

task install(dependsOn: uploadArchives)